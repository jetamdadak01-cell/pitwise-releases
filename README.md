# PitWise releases

This repository hosts **only the version manifest** (`version.json`) used by the PitWise app to notify customers about new versions.

There are **no product files here**. PitWise is a paid app - get it at https://pitwise.net

When the app detects a newer version, it points the customer to their original Payhip download link, which always serves the latest build.
